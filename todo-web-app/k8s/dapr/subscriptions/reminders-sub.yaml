# [Task]: T-5.1.4 — Subscription: reminders → notification-service (Phase 5.2)
# Using v2alpha1 (cluster default)
apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: reminders-subscription
  namespace: default
spec:
  pubsubname: todoai-pubsub
  topic: reminders
  routes:
    default: /api/events/reminder
