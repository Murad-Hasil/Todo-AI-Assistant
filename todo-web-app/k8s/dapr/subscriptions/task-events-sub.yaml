# [Task]: T-5.1.2 — Subscription: task-events → todo-backend
# Using v2alpha1 (cluster default — v1alpha1 requires unavailable conversion webhook)
# Phase 5.2 will add the /api/events/task handler in FastAPI
apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: task-events-subscription
  namespace: default
spec:
  pubsubname: todoai-pubsub
  topic: task-events
  routes:
    default: /api/events/task
